[project]
name = "cert-tutor"
version = "0.1.0"
description = "Offline CLI study tutor for technical certifications"
requires-python = ">=3.12"
dependencies = [
    "rich>=13.0.0",
    "pyyaml>=6.0",
    "prompt-toolkit>=3.0.52",
]
