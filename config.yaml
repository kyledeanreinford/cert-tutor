session:
  data_dir: "data"
  history_file: "data/session.json"

tutor:
  weak_spot_threshold: 0.5
  recent_n: 30
  difficulty:
    default_level: 1
    promote_threshold: 0.8
    demote_threshold: 0.4
    window_size: 10
